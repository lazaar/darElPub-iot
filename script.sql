CREATE TABLE IF NOT EXISTS video(id INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE, name VARCHAR(30), qte INTERGER, show BOOLEAN)
CREATE TABLE IF NOT EXISTS diffusion(id INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE, number INTERGER, date DATE, id_video INTEGER FOREIGN KEY REFERENCES video(id))